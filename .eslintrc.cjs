// We use a JavaScript file instead of YAML so that this repository gets
// categorized as JavaScript by GitHub.
// eslint-disable-next-line unicorn/prefer-module, import/no-commonjs, import/unambiguous, filenames/match-regex
module.exports = {
  extends: '@ehmicky/eslint-config/.eslintrc.yml',
}
